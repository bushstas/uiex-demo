import React from 'react';
import Demo from '../../Demo';
import {Tab} from 'uiex/Loader';

export default class TabDemo extends Demo {
	static map = {
		checkboxes: {
		
		},
		inputs: [

		]
	};
	static data = {
		color: 'black'
	};
	static excluded = ['vertical', 'block', 'valign', 'height'];
	static handlers = [];
	static stateProps = [];
	static funcs = {
		
	};
	static args = {
		
	};
	static componentName = 'Tab';
	static component = Tab;

}